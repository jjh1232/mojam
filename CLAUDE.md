# Prelaps (`prelaps.com`)

브라우저 안에서만 도는 텍스트 도구 모음. **첫 번째 도구가 「깨진 글자 복구기」**(`/mojibake`)이고, 이 문서의 대부분은 그 엔진 얘기다. 도메인에 도구가 여러 개 산다는 전제가 URL·푸터·법적 페이지 규칙을 결정하므로 「도메인과 URL」 절을 먼저 볼 것. 상세 기획은 @docs/기획서.md 참고.

## 절대 규칙

- **엔진 원본은 `engine.js` 이다.** 예전엔 `index.html` 안에 있었고 `test/sync.js` 가 사본을 떠서 테스트에 썼는데, 다국어로 가면서 언어별 페이지가 엔진을 공유해야 해 밖으로 뺐다. 사본이 없어졌으므로 `test/sync.js` 도 삭제했다 — 테스트가 `engine.js` 를 직접 `require` 한다.
- **`engine.js` 를 `<script type="module">` 로 부르지 말 것.** `file://` 에서 CORS 로 막힌다(실측 확인). 일반 `<script src>` 는 하위 폴더의 `../engine.js` 까지 정상 로드된다. 이 프로젝트는 더블클릭으로 열리는 것이 원칙이라(기획서 3.1) 이 구분이 중요하다.
- **엔진에 언어별 분기를 넣지 말 것.** `langHint()` 는 `'ko'`·`'cyrillic'` 같은 **키**만 돌려주고, 사람이 읽는 글자는 화면 코드의 `I18N.lang` 이 만든다. 벤치마크도 **키로 검사**하므로 번역을 고쳐도 안 깨진다.
- **엔진 수정 후 반드시 `npm test` 실행.** 링크 전수 검사 → 붙여넣기·파일 두 모드를 Node·브라우저에서 각각 돈다. 하한 미달이면 커밋 금지. **페이지만 고쳤을 때는 `npm run check:links` 만 돌려도 된다**(2초).
  - 붙여넣기: Node 80% / 브라우저 82% (현재 82.4% / 84.5%)
  - 파일: Node·브라우저 74% (현재 76.6% / 75.3%, 후보 3개 내 81.8% / 88.3%)
- **정확도를 인용할 때는 브라우저 숫자를 쓴다.** Node 벤치마크는 빠른 게이트일 뿐 실사용 값이 아니다 — 아래 참고.
- **두 모드의 숫자를 섞어 인용하지 말 것.** 케이스 집합이 다르다. 파일 벤치는 단일바이트 인코딩 12종을 동등 비중으로 재는데 그 구간이 원리적 동점이라 전체 숫자를 끌어내린다. 같은 CJK 구간만 보면 파일 모드가 훨씬 높다(utf-8·euc-kr·gbk 각 100%).
- **모든 텍스트 처리는 클라이언트에서.** 서버 전송 코드 작성 금지 — "입력이 서버로 가지 않는다"가 이 도구의 차별점이다.
- **프레임워크 도입 금지.** React/Vue/Next 불필요. 사이트화 시에도 Node 빌드 스크립트만 사용.
- `localStorage` / `sessionStorage` 사용 금지.

## 파일 모드를 건드릴 때

붙여넣기와 파일은 **입력의 성격이 다르므로 진입점이 다르다.** `recoverBytes()` 는 `attempt()` 를 우회하고 파일 바이트를 `ACTUAL` 로 바로 디코딩한다. `encodeWith` 역추정 단계가 없으므로 손실이 없다.

**"깨진 파일" 과 "내용이 깨진 파일" 은 다른 문제다.** 둘을 헷갈리면 안 된다.

| | 깨진 파일 | 내용이 깨진 파일 |
|---|---|---|
| 상태 | 바이트가 euc-kr 인데 utf-8 로 읽힘 | 바이트는 정상 utf-8, 담긴 *글자*가 이미 mojibake |
| 어떻게 생기나 | 옛날 프로그램이 저장 | **깨진 글자를 복사해 메모장에 저장** |
| 처리 | `decodeCandidates` 경쟁 | `contentMojibake()` → `recover()` 체인 |

두 번째가 **실사용에서 더 흔하다.** 사용자는 깨진 글자를 어디선가 복사해 파일로 만들어 올린다. 이걸 놓치면 "정상 파일입니다" 라고 답하고 끝나 아무 쓸모가 없다.

- **`validUtf8()` 확정 경로를 지우지 말 것.** 유효 UTF-8 을 경쟁에 맡기면 내용이 깨진 UTF-8 파일이 **gbk 로 넘어간다** — 깨진 글자는 라틴 보조 문자뿐이라 `score()` 가 낮게 주는데, 같은 바이트를 GBK 로 읽으면 한자가 나와 110점을 받기 때문이다. `strictUtf` 가점 +70 으로는 못 막는다. 파일 벤치의 비 UTF-8 케이스 57건 중 유효 UTF-8 인 것은 **0건**이라 이 확정 규칙은 기존 정확도를 건드리지 않는다.
- 서로 **다른 방식으로 깨진 글자가 한 파일에 섞이면 통짜로는 복구 불가**다 — 복구는 "어떻게 잘못 읽혔는지" 하나를 가정하고 되감는 것이라 가정이 여러 개면 `encodeWith` 가 전부 null 을 낸다. **줄 단위로 나누면 풀린다**(`recoverLines`, 아래 절 참고). 그것마저 실패했을 때만 `MOJI_HINT` 로 사유를 안내한다.

- **`score()` 는 공유하고 절대 건드리지 말 것.** 파일 모드 조정은 `FILE_PRIOR` 와 가드 함수(`sniffBom`/`looksBinary`/`hasHighByte`)로만 한다. 여기를 고치면 붙여넣기 82.4% / 84.5% 가 같이 흔들린다.
- **`attempt()` 의 길이 축소 가점(5차)을 파일 모드로 옮기지 말 것.** 실측으로 기각했다. 그 가점은 "입력이 이미 깨진 것이 확실할 때 정답은 짧다"는 전제인데, 파일에는 그 전제가 없다. 오히려 단일바이트 파일을 CJK 로 읽으면 글자 수가 반이 돼 가점을 챙긴다. Chrome 은 euc-kr 을 Node 의 두 배로 매핑해 이 오답이 훨씬 잘 살아남는다 — 제거하니 브라우저 68.8% → 75.3%, 독일어 29% → 100%.
- **`FILE_PRIOR` 는 동점 깨기 전용이다.** 단일바이트 인코딩 12종은 `score()` 상 **완전 동점**이 난다(그리스어·키릴·라틴 악센트 전부 스크립트 110점). 가점을 안 주면 배열 순서로 windows-1251 이 항상 이겨 서유럽 파일이 전부 키릴로 나온다. 최대 6점이라 진짜 신호(보통 45점 단위)는 뒤집지 못한다. 66.2% → 76.6%.
- **파일 모드에서도 인코딩 후보 확장은 이득이 없다.** `windows-874`·`iso-8859-8`·`windows-1258`·`iso-8859-5` 를 넣어봐도 1순위·3개내 **둘 다 무변화**였다. 붙여넣기 모드와 같은 결론이다.
- 단일바이트끼리의 오판은 원리적 한계다(기획서 M6 언어 모델 대상). 케이스를 빼지 말고 숫자를 그대로 둘 것.

### ZIP 파일명

- **압축 데이터는 절대 건드리지 않는다.** `zipRebuild` 는 데이터 블록을 그대로 복사하고 헤더만 새 이름 + GP bit 11 로 다시 쓴다. inflate/deflate 가 필요 없어 외부 라이브러리도 없다. 여기에 압축 라이브러리를 들이지 말 것.
- extra 필드는 버린다 — Unicode Path(0x7075) 잔재가 새 이름과 충돌한다.
- 거부 조건: ZIP64(EOCD 필드 포화 또는 extra 헤더 0x0001), 암호화(GP bit 0), 분할 아카이브. 조용히 처리하지 말고 사유를 표시할 것.
- 파일명은 **전부 이어붙여 한 덩어리로 판별한다.** 이름 하나는 짧아 통계가 성립하지 않는다 (기획서 5.1.2 의 짧은 입력 한계와 같은 문제).

## 채점 로직을 건드릴 때

`score()` 의 배점은 언어 간 균형이 미묘하다. 한 언어를 고치면 다른 언어가 조용히 깨진다.

- 배점 하나만 바꾸고 `npm test` 로 언어별 표를 확인할 것. 여러 개 동시에 바꾸지 말 것.
- 인코딩 후보를 늘리는 것은 대체로 **역효과**다. 18→36종 확장이 84.2%→81.6% 하락을 낳았음. 후보 추가 제안 전에 벤치마크로 증명할 것.
- 새 언어 지원 요청이 오면 `ACTUAL` 에 인코딩을 추가하기 전에 `bucket()` 의 문자 분류부터 확인. 정답이 `latinodd`(-150)나 `rare`(-260)로 분류되고 있을 가능성이 높다.
- **부분 복구(`` 포함 후보)를 순위 경쟁에 넣지 말 것.** 별도 섹션으로 분리돼 있다. 랭킹에 섞으면 정상 입력에서 정답 위에 올라간다. 채점으로 이기게 만들려면 배점을 74점 흔들어야 하는데 그러면 러시아어가 무너진다.
- 비율 기반 채점은 **글자 수가 불어난 오답에 유리**하다. 멀티바이트 정답이 단일바이트 오답에 밀리면 `score()` 배점보다 `attempt()` 의 길이 축소 가점을 먼저 의심할 것.

## 벤치마크를 건드릴 때

- **Node 의 `TextDecoder` 와 브라우저의 `TextDecoder` 는 표가 다르다.** Node 는 ICU, 브라우저는 WHATWG index 를 쓴다. euc-kr 기준 Node 8,412쌍 / Chrome 17,048쌍으로 두 배 차이가 나고, big5·gbk 도 어긋난다. 엔진은 이 표를 그대로 신뢰하므로 **Node 벤치마크만 보면 엔진을 과소평가한다** (`utf-8→euc-kr` 이 Node 0/5, 브라우저 5/5). 실사용 값은 `npm run bench:browser`.
- 케이스 정의는 `test/cases.js` 한 곳에만 둔다. `benchmark.js`(Node)·`filebench.js`(Node 파일)·`browser.js`(Chrome, 두 모드 다)가 공유하므로 한쪽에만 추가하지 말 것.
- **깨진 글자만 딱 떼어낸 입력만 재면 실사용을 못 본다.** 그렇게만 재던 시절 82.4% 였던 것이 앞에 `제목: ` 만 붙이면 0% 였다. `buildWrapped()` 가 래퍼 5종을 씌워 이 구간을 잰다 — 새 기능을 넣을 때 통짜 케이스만 보고 판단하지 말 것.
- **`test/browser.js` 는 Chrome/Edge(둘 다 Chromium)만 띄운다.** "브라우저 실측" 은 엄밀히 Chromium 실측이다. 파폭·사파리는 손으로 열어 확인한다 — geckodriver 의존성을 들일 만한 문제가 아니다. `TextDecoder` 는 WHATWG 가 인덱스 표를 규정하므로 브라우저끼리는 갈리지 않는다. 다른 표(ICU)를 쓰는 것은 Node 뿐이다.
- **파일 모드는 정답 조건이 다르다.** 인코딩 "이름"이 아니라 디코딩 결과 문자열이 원문과 같은지로 본다 (같은 바이트를 gbk/gb18030 처럼 호환 인코딩으로 읽어 같은 글자가 나오면 정답). 그리고 깨짐 시뮬레이션이 없다 — 파일은 원본 바이트 그 자체다.
- **인코딩 라벨은 `iconv-lite` 로 통일한다. `TextDecoder` 를 섞지 말 것.** WHATWG 표준에서 `latin1`·`iso-8859-1` 은 전부 `windows-1252` 의 별칭이라 진짜 ISO-8859-1 이 아니다. 섞어 쓰면 왕복 검사가 어긋나 케이스가 **조용히 전부 탈락**한다 (실제로 `euc-kr→latin1` 이 0건이었다).
- 정확도 숫자를 인용하기 전에 **쌍별 커버리지 표부터 볼 것.** 전체 정답률은 어떤 쌍이 몇 건인지에 좌우된다. 0건인 쌍은 "잘한 것"이 아니라 "안 잰 것"이다.
- 케이스가 실패한다고 CASES 에서 빼지 말 것. 낮은 숫자를 그대로 쓰고 원인을 기록한다.

## 다국어 작업 시 (현재 최우선 과제)

**구조는 다 잡혔다. 언어를 늘리는 일은 `i18n.<언어>.js` 하나를 번역하고 페이지 한 장을 만드는 것뿐이다.**

- **새 언어를 추가하는 절차** — ① `i18n.ko.js` 를 복사해 번역 ② `ja/` 폴더를 통째로 복사해(허브 `index.html` + 도구 `mojibake.html` + `content/` 4장) 본문(글)과 `<head>` 를 번역 ③ 스크립트 경로의 언어 파일만 갈아끼움 ④ **모든 페이지의 `.langsw` 에 링크를 추가** ⑤ `hreflang` 줄을 18장 전부에 추가 ⑥ `sitemap.xml` 에 6줄 추가. `engine.js` 와 `ui.js` 는 **절대 손대지 않는다.** 끝나면 `npm run check:links` 로 확인한다.
- **새 도구를 추가하는 절차** — ① `mojibake.html` 을 복사해 `<도구>.html` 로(3언어) ② 각 언어 허브의 `.tools` 에 카드 추가 ③ 새 페이지 4장에 canonical·hreflang·og ④ `sitemap.xml` 에 3줄 ⑤ `npm run check:links`. **법적 페이지는 건드리지 않는다** — 도메인 전체 공용이라 이미 새 도구를 덮는다.
- **`.langsw` 는 페이지마다 고유하다.** 상대 경로가 깊이에 따라 다르고(루트는 `en/mojibake.html`, 하위는 `../en/content/terms.html`), 현재 언어만 `<span aria-current>` 이며, 무엇보다 **가리키는 대상이 "같은 페이지의 다른 언어" 여야 한다** — 도구 A 의 영어 링크는 도구 A 의 영어판이지 영어 허브가 아니다. 도구가 늘수록 여기가 가장 먼저 어긋난다. **`npm run check:links` 가 이 셋을 전부 검사하므로 눈으로 확인하지 말 것.**
- **폴더 이름은 나라 코드가 아니라 언어 코드다.** `kr`·`jp` 가 아니라 `ko`·`ja` — 구글 `hreflang` 은 언어 코드만 받고, 나라 코드를 쓰면 태그가 무효 처리돼 다국어 SEO 가 통째로 무의미해진다. **한국어는 루트에 두고** 따로 `/ko/` 를 만들지 않는다(중복 콘텐츠).
- **`ui.js` 에 사람이 읽는 글자를 직접 쓰지 말 것.** 하나라도 박으면 그 문장만 모든 언어에서 한국어로 나온다. 확인법: `ui.js` 에서 한글이 든 문자열 리터럴을 찾아 0건이어야 한다(주석은 제외).
- UI 문자열은 `I18N` 객체로 분리한다(완료). 엔진 코드에 언어별 분기를 넣지 말 것 — `langHint()` 는 `'ko'`·`'cyrillic'` 같은 **키**만 돌려주고 글자는 `I18N.lang` 이 만든다.
- **숫자가 끼어드는 문장은 `I18N` 에서 함수로 둔다.** 언어마다 어순이 달라 호출부에서 이어붙이면 번역이 불가능해진다 (`shortInput: (n,min) => ...`).
- **칩 뒤에 붙는 낱말은 후치로 자연스러운 것을 고를 것.** 마크업이 `[칩] 글자` 순서로 고정돼 있어서, 한국어 `[utf-8] 로 읽기` 를 영어로 `read as` 라고 옮기면 `[utf-8] read as` 로 거꾸로 읽힌다. `decoded`·`reversed` 처럼 뒤에 와도 말이 되는 낱말을 쓴다.
- **입력창 우측 상단에 언어 이름 버튼을 두지 말 것.** 예시 로더로 만들었다가 언어 전환기로 오인돼 걷어냈다. 그 자리에 언어명이 보이면 사용자는 UI 언어 탭으로 읽는다.
  - **지금 그 자리에는 `지우기` 가 있고, 언어 전환은 페이지 최상단(`.langsw`)에 있다.** 규칙과 어긋나지 않는다 — 오인의 원인은 위치가 아니라 "그 자리에 **언어 이름**이 보이는 것" 이었다.
- **언어 이름은 번역하지 말 것.** 어느 페이지에서든 `한국어` · `English` 로 각 언어의 자기 이름을 쓴다. 영어 페이지에서 한국어가 `Korean` 으로 보이면 **한국어만 읽는 사용자가 자기 언어를 못 찾는다.** 그래서 `I18N` 에 넣지 않고 두 페이지에 같은 마크업을 둔다.
- **`.langsw` 에 `.brand` 를 재사용하지 말 것.** `.brand` 는 18장 전부의 맨 위 제목 블록(눈썹 + 사이트 이름)이 쓰고 있다. 한 클래스명을 두 용도로 쓰면 뒤에 온 정의가 이긴다 (`.copy` 전례).
  - 예전엔 `.tag` 가 그 자리였다 — 도구 페이지에서는 눈썹 글자, 법적 페이지에서는 `.tag a` 로 "도구로 돌아가기" 링크를 겸했다. `.brand` 로 통일하면서 `.tag` 는 **눈썹 전용**이 됐고 `.tag a` 규칙은 삭제했다. 되살리지 말 것.
- 기계번역 결과를 검수 없이 커밋하지 말 것. 애드센스가 저품질 콘텐츠로 판정할 위험이 있다.
- **본문에 깨진 문자열 예시를 다시 넣지 말 것.** 한 번 넣었다가 걷어냈다 — 실제 사용자는 자기 깨진 글자를 갖고 오므로 예시가 필요 없고, 그게 도구 바로 아래 자리(701자)를 차지해 정작 실패한 사용자가 봐야 할 내용을 밀어냈다. 다시 넣는다면 그때 `test/examples.js` 같은 검증도 같이 살려야 한다.
  - 그때 알아둔 것 두 가지: `&#x80;` 같은 **숫자 문자 참조로 C1 을 쓰면 HTML5 가 windows-1252 로 재매핑**해 다른 글자가 된다(`&#x80;` → `€`). 문자를 그대로 박으면 브라우저가 온전히 보존한다. 그리고 C1·NBSP 가 든 샘플은 메신저·터미널을 거치면 소실된다.
- **사람에게 예시 문자열을 보여줄 때 축약형(`…`)을 쓰지 말 것.** 페이지 본문이든 대화든 마찬가지다. 사람은 눈앞의 문자열을 그대로 복사한다. 실제로 답변 표에 `¾È³çÇÏ¼¼¿ä…` 를 실었더니 그대로 붙여넣어 실패했다 — **잘린 입력이라 당연히 안 되는데 도구가 고장 난 것처럼 보인다.** 길어도 전체 문자열을 싣고, 줄바꿈으로 감쌀 것.
- `<html lang>` 과 메타태그도 언어에 따라 바뀌어야 한다. 이게 빠지면 다국어를 해도 검색 유입이 안 생긴다.

## 본문을 고칠 때

본문을 읽는 사람은 **이미 도구를 써보고 실패한 사람**이다. 들어오자마자 붙여넣고, 안 되니까 스크롤한 것이다. 그 순서에 맞춰 배치돼 있다.

```
① 복구가 안 되나요      증상 → 원인 → 대처 표.  가장 두껍다 (792자)
② 뷁어·뷁체가 뭔가요     현상의 이름과 원인.  검색어이기도 하다
③ 붙여넣기보다 파일이 정확
④ 어떻게 동작하나요
⑤ 자주 묻는 것
```

- **①을 맨 앞에서 밀어내지 말 것.** 예전에 "이런 글자를 넣으면 됩니다"(예시 표)가 그 자리를 차지하고 "안 될 때"는 347자로 가장 짧았다. 순서와 비중이 거꾸로였다.
- **ftfy 벤치마크 비교를 index 에 다시 넣지 말 것.** 개발자용 자랑이라 방금 실패한 사람에게 쓸모가 없다. `content/about.html` 에 있으니 지운 게 아니라 옮긴 것이다.
- **`자주 묻는 것` 의 문항을 고치면 `<head>` 의 `FAQPage` 구조화 데이터도 같이 고칠 것.** 화면과 구조화 데이터가 어긋나면 구글이 무시한다. **`npm run check:links` 가 질문·답변을 글자 단위로 대조하므로 눈으로 확인하지 말 것** — 규칙으로만 적어뒀던 시절 3언어 15문항 중 13문항이 어긋난 채로 있었다(19차에 발견).
- 본문 분량은 **1,500자 이상**을 유지한다 (기획서 4장). 현재 2,224자.

## 도메인과 URL (`prelaps.com`)

**이 도메인에는 도구가 여러 개 산다.** 깨진 글자 복구기는 그중 첫 번째일 뿐이고, 인코딩 계열 도구와 무관한 도구가 같이 들어온다. 아래 규칙이 전부 거기서 나온다.

```
prelaps.com/                    허브(도구 목록)      index.html
prelaps.com/mojibake            깨진 글자 복구기      mojibake.html
prelaps.com/mojibake-csv        (M4) 계열 확장       mojibake-csv.html
prelaps.com/<무관한도구>          접두어 없이
prelaps.com/content/terms       법적 4장 — 도메인 전체 공용
prelaps.com/en/ · /ja/          위 구조를 언어별로 그대로
```

- **언어가 첫 칸, 도구가 두 번째 칸이다.** 반대로 `/mojibake/en/` 로 하면 `hreflang` 세트와 sitemap 이 도구 수만큼 쪼개진다.
- **도구는 파일식(`mojibake.html`), 폴더식(`mojibake/`) 금지.** 기획서 3.1 의 더블클릭 원칙이 첫째 이유이고, 둘째 이유는 **경로 깊이가 2 로 고정되어 푸터가 6종으로 유지된다**는 것이다. 폴더식으로 가면 깊이 3 이 생겨 푸터가 9종이 된다.
- **계열 접두어는 인코딩 계열에만 붙인다** (`mojibake-csv`). 무관한 도구가 섞이는 도메인이라 접두어가 없으면 루트가 주어 없는 이름들로 차서 나중에 구분이 안 된다.
- **slug 는 3언어 전부 같은 영어 소문자다.** 언어별로 다르게 하면 `hreflang` 매핑을 손으로 관리해야 하고 한 칸 틀리면 조용히 무효가 된다. 한글 URL 은 퍼센트 인코딩돼 공유할 때 링크가 깨진다.
- **법적 페이지는 도구별로 복제하지 말 것.** `content/` 4장 × 3언어 = 12장이 **도메인 전체 공용**이다. 그래서 약관·개인정보처리방침의 적용 범위 문장이 도구 이름이 아니라 `Prelaps(prelaps.com)와 이 사이트에 올라온 모든 도구` 로 쓰여 있다 — 도구 이름으로 되돌리면 두 번째 도구를 안 덮는다.

### 배포 URL 은 확장자가 없다

Cloudflare Pages·Netlify 는 기본으로 `.html` 을 떼고 301 한다 (`/mojibake.html` → `/mojibake`).

- **내부 링크는 `.html` 을 유지한다** — `file://` 더블클릭 원칙 때문이다.
- **`canonical`·`og:url`·`sitemap.xml` 은 확장자 없는 최종 URL** 을 쓴다. `.html` 을 적으면 리다이렉트를 가리키게 된다.
- 둘의 형태가 다른 것은 실수가 아니다. `test/links.js` 가 이 매핑(`index.html` → `/`, `그밖에.html` → `/그밖에`)을 그대로 검사한다.
- **배포 직후 실제 응답을 눈으로 확인할 것.** `.html` 을 안 떼는 호스팅으로 옮기면 위 규칙이 통째로 뒤집힌다. `_headers` 확인할 때 같이 본다.
- **Cloudflare Pages 설정은 세 칸뿐이다** — 프레임워크 프리셋 `None` · 빌드 명령 **비움** · 빌드 출력 디렉터리 **`site`**. 빌드 명령에 뭔가를 적으면 그 순간 "원본과 산출물 두 벌" 이 생긴다. 비워두는 것이 이 프로젝트의 전제다.

### `hreflang` · `canonical`

18장 전부 `<head>` 에 canonical 1줄 + hreflang 4줄(ko·en·ja·x-default) + `og:url` + `og:image` 가 있다.

- **`x-default` 는 `/en/` 계열이다.** 언어가 안 맞는 방문자를 보낼 곳이라 영어가 맞다. 한국어가 루트인 것과는 별개 문제다.
- **`hreflang` 은 "같은 페이지의 다른 언어" 여야 한다.** 도구가 늘면 여기가 가장 먼저 어긋나는 자리다 — 도구 A 의 영어판은 도구 A 여야 하는데 습관적으로 `en/index.html` 을 적기 쉽다. `npm run check:links` 가 이걸 잡는다.
- `og:image` 는 절대 URL 이어야 한다. 상대 경로는 대부분의 미리보기 생성기가 무시한다.
- **www 는 쓰지 않는다.** `https://prelaps.com` 로 통일하고 `www` 는 리다이렉트한다.

## 사이트 구조 (빌드 없음)

**`site/` 안이 그대로 사이트다.** 변환 단계가 없으므로 아무 파일이나 브라우저로 바로 열면 그게 실제 화면이다.

- **`site/` 는 빌드 산출물이 아니라 원본이 사는 곳이다.** 복사본이 아니므로 "어디를 고치냐" 가 갈리지 않는다 — 고칠 곳은 언제나 `site/` 안이다. `dist/` 처럼 생성되는 폴더로 오해하지 말 것.
- **`site/` 를 만든 이유는 배포 격리다.** 저장소 루트를 그대로 배포하면 `docs/기획서.md`(수익 목표·미결정 사항)와 `test/`·`CLAUDE.md` 가 같이 올라가 색인된다. Cloudflare Pages 의 **빌드 출력 디렉터리를 `site` 로** 지정하면 빌드 명령 없이 이것만 배포된다.
- **`site/` 안팎으로 파일을 옮길 때는 상대 경로를 확인할 것.** 트리를 통째로 옮기는 한 안 깨지지만(실제로 그렇게 이사했다), 한 파일만 밖으로 빼면 그 파일을 부르는 18장이 조용히 죽는다.

```
site/
index.html          한국어 허브.  도구 목록.  도구 코드를 안 부르므로 <script> 가 없다
mojibake.html       한국어 도구.  본문(글)만 한국어이고 나머지는 아래 셋을 부른다
en/index.html       영어 허브
en/mojibake.html    영어 도구.  ../engine.js · ../i18n.en.js · ../ui.js 를 부른다
ja/index.html       일본어 허브
ja/mojibake.html    일본어 도구

engine.js           복구 엔진.  언어 중립이라 모든 언어가 공유한다
ui.js               화면 코드.  글자를 직접 안 쓰고 I18N 에서 꺼내므로 역시 공유한다
i18n.ko.js          한국어 화면 문자열.  **번역 대상은 이 파일 하나다**
i18n.en.js          영어 화면 문자열
i18n.ja.js          일본어 화면 문자열

styles.css          모든 페이지가 공유
content/*.html      법적 페이지 4개(한국어).  완성된 페이지이지 조각이 아니다
en/content/*.html   영어 법적 페이지 4개
ja/content/*.html   일본어 법적 페이지 4개
robots.txt          sitemap.xml 위치를 가리킨다
sitemap.xml         18장 전부.  절대 URL 이라 도메인이 박혀 있다
_headers            보안 응답 헤더 (Cloudflare Pages · Netlify 가 읽는다)
favicon.svg         탭 아이콘
og-image.png        1200×630. 링크 미리보기용
```

**페이지는 `engine.js` → `i18n.<언어>.js` → `ui.js` 순서로 부른다.** `ui.js` 가 `I18N` 을 읽으므로 순서를 바꾸면 안 된다.

**배포 대상은 `site/` 폴더 하나다.** 그 안의 것을 골라 올리는 것이 아니라 폴더째 올라간다 — 예전에는 14개를 손으로 골라야 했고 `_headers` 를 빠뜨리는 사고가 여기서 났다.

페이지는 **18장**이다 (허브 3 + 도구 3 + 법적 4×3).

- **허브에는 `<script>` 를 넣지 말 것.** 허브는 도구가 아니라 목차라 `engine.js`·`ui.js`·`i18n.*.js` 를 부를 이유가 없다. 붙이면 루트 방문자가 쓰지도 않을 24KB 를 매번 받는다.

- **공유 `.js` 셋을 빠뜨리면 도구가 통째로 안 뜬다.** `_headers` 와 달리 즉시 눈에 보이는 고장이라 그나마 낫지만, 로컬에서 페이지를 복사해 열 때도 같이 옮겨야 한다(실제로 한 번 빠뜨렸다).
- **법적 페이지의 준거법·아동 조항은 번역이 아니라 판단이 들어간 자리다.** 준거법은 운영자가 한국에 있으므로 영어·일본어판도 **대한민국 법률 그대로** 둔다(바꾸면 사실이 틀려진다). 한국어판의 "만 14세 미만" 은 국내법 기준이라 그대로 옮기지 않고 **"연령을 불문하고 개인정보를 수집하지 않는다"** 로 서술했다 — 영어권 COPPA 13세, GDPR 13~16세 등 기준이 달라 나이를 못 박으면 틀린 진술이 된다.
`test/` · `docs/` · `node_modules/` · `package.json` · `CLAUDE.md` · `README.md` 는 `site/` 밖에 있어 자동으로 빠진다.

- **favicon 에 `<text>` 로 글자를 넣지 말 것.** 보는 사람 컴퓨터의 글꼴에 좌우되고 **한글 글꼴이 없는 환경에서는 두부(□)로 보인다.** mojibake 복구 도구가 두부를 아이콘으로 다는 건 곤란하다. 도형(`rect`)만 쓴다.
- **`og-image` 는 PNG 여야 한다.** 카톡·페이스북 미리보기 생성기는 SVG 를 대체로 렌더링하지 않는다. 다시 만들 일이 생기면 HTML 카드를 headless Chrome 으로 1200×630 스크린샷 찍는 방식을 쓴다(테스트에서 쓰는 것과 같은 방법이라 새 도구가 필요 없다).
- **`og:image` 는 절대 URL 이다** (`https://prelaps.com/og-image.png`). 상대 경로를 넣으면 대부분의 미리보기 생성기가 무시한다. `canonical`·`og:url`·`sitemap` 과 함께 18장 전부에 들어가 있다 — 「도메인과 URL」 절 참고.

- **`_headers` 를 빠뜨리지 말 것.** 안 올리면 헤더가 통째로 안 붙는데 **화면상 아무 표시가 없다.** 배포 후 개발자도구 Network 탭에서 응답 헤더를 눈으로 확인하는 수밖에 없다.
- **`Content-Security-Policy` 를 지금 넣지 말 것.** 제대로 하려면 Google Fonts 와 애드센스 스크립트를 전부 허용 목록에 올려야 하는데, 하나 빠뜨리면 **광고가 조용히 안 뜬다.** 광고를 붙이는 시점에 실제로 어떤 도메인을 부르는지 보고 추가한다.
- **캐시 헤더도 넣지 말 것.** `styles.css` 에 버전 표시가 없어서 길게 캐시하면 고쳐도 예전 것이 남는다.
- **AI 크롤러는 막지 않는다.** 막으면 유입 경로 하나를 스스로 닫는 셈이고, 애드센스 수익에도 유리하지 않다. 콘텐츠 도용이 실제로 문제가 되면 그때 판단한다.

- **빌드 스크립트를 다시 만들지 말 것.** 한 번 만들었다가 걷어냈다. 기획서 6.1 에 "build.js 로 정적 HTML 생성" 이라고 적혀 있다는 이유로 규모를 따져보지 않고 만든 것이 원인이었다. 5페이지짜리 사이트에서 빌드가 버는 것은 푸터 11줄뿐인데, 그 대가로 **"원본과 산출물 두 벌"** 이라는 개념이 생겨 "약관을 어디서 고치냐" 는 질문이 세 번 반복됐다.
- **맨 위 한 줄(`.topbar`)은 18장에 다 있다.** `.wrap` 의 첫 요소이고 **왼쪽이 제목 블록(`.brand`), 오른쪽이 언어 전환기(`.langsw`)** 다. `.brand` 는 눈썹 `PRELAPS`(사이트 이름) + 그 페이지 이름을 감싼 링크로, **어느 페이지에서든 눌러 자기 언어의 허브로 돌아간다** — 검색으로 약관 페이지에 바로 들어온 사람이 푸터까지 안 내려가도 되게 하는 자리다.
  - **눈썹은 사이트 이름, 그 아래는 페이지 이름이다.** 예전엔 눈썹이 `MOJIBAKE RECOVERY`(도구 이름)였는데 도메인에 도구가 여러 개 살게 되면서 사이트 이름으로 바꿨다. 도구 이름을 눈썹으로 되돌리지 말 것 — 무관한 도구 페이지에서 거짓말이 된다.
  - **`.langsw` 를 `.brand` 앞에 두지 말 것.** 소스 순서가 곧 키보드 `Tab` 순서라, 로고보다 언어 전환기가 먼저 잡히면 어색하다. 화면상 오른쪽 정렬은 `margin-left:auto` 가 하므로 순서를 바꿀 이유가 없다.
  - **제목 블록을 `header` 안으로 되돌리지 말 것.** 한 번 그렇게 만들었다가 옮겼다 — 전환기가 맨 위 한 줄을 혼자 차지해 제목이 44px 아래로 밀렸고, 도구가 그만큼 늦게 보였다. 지금 배치는 `base64decode.org` 계열 도구와 같다.
  - **경로는 두 종류뿐이다** — 허브·도구 페이지에서 `index.html`, 법적 페이지 12장에서 `../index.html`. 자기 언어의 허브를 가리키며, 하위 폴더는 이 상대 경로가 이미 자기 언어를 가리킨다. `aria-current="page"` 는 **허브 3장에만** 붙는다(자기 자신이므로).
  - **법적 페이지에서 사이트 이름을 `<h1>` 으로 쓰지 말 것.** 페이지마다 `<h1>` 은 하나이고, 법적 페이지의 그 하나는 흰 종이 맨 위의 페이지 제목(`이용약관` 등)이다. 로고 안의 사이트 이름은 `.bname`(22px)이라 h1(29px)보다 작다 — 크기가 뒤집히면 구글이 그 페이지 주제를 사이트 이름으로 읽는다.
  - **설명 문장은 도구 페이지에만 둔다.** 법적 페이지에 복제하면 페이지 주제가 흐려지고 본문 시작이 아래로 밀린다. 설명 문장은 `.topbar` 가 아니라 그 아래 `header` 안에 있다.
  - **로고에 마우스를 올릴 때 밑줄을 긋지 말 것.** 굵은 제목 아래로 선이 죽 이어져 보기 싫다는 지적을 받아 색 변화(`--accent`)로 바꿨다.

- **푸터는 18개 파일에 있지만 내용은 6종뿐이다.** 고칠 때는 **6종을 먼저 정하고 해당 파일들에 뿌리는** 순서로 한다 — 18번 따로 쓰면 그중 하나가 어긋난다.
  ```
  index.html · mojibake.html            ┐
  en/index.html · en/mojibake.html      ├ 언어별 2장.  같은 깊이라 푸터가 바이트 동일
  ja/index.html · ja/mojibake.html      ┘
  content/*   ·  en/content/*   ·  ja/content/*           ← 언어별 4장이 서로 바이트 동일
  ```
  6종인 이유는 번역(3) × 경로 깊이(2) 다. **도구가 늘어도 6종은 그대로다** — 도구 페이지는 전부 자기 언어 루트에 있어 깊이가 안 늘어난다(위 "폴더식 금지" 가 지키는 것이 이것이다). 깊이가 두 벌인 것은 **`file://` 더블클릭 원칙의 대가**이고(기획서 3.1), 루트 기준 경로로 바꾸면 3종이 되지만 로컬에서 링크가 전부 죽는다.
- **JS 로 푸터를 그리지 말 것.** 중복은 사라지지만 개인정보처리방침·약관 링크가 HTML 소스에서 빠진다. 애드센스 체크리스트가 요구하는 것이 그 링크다(기획서 7장). 간단해 보여 다시 꺼내기 쉬운 안이라 여기 적어둔다.
- **대신 푸터를 고치면 18개 파일을 다 고쳐야 한다.** 빌드를 없앤 대가는 이것 하나다. 확인법:
  ```
  cd site && grep -lc fnav *.html en/*.html ja/*.html content/*.html en/content/*.html ja/content/*.html
  ```
- **링크는 눈으로 검사하지 말 것.** 18장 × 상대 경로라 깨져도 화면에 표시가 없다. `npm run check:links`(`test/links.js`)가 **0건**이어야 한다. 무엇을 보는지:
  ```
  상대 링크가 실제 파일을 가리키는가        <html lang> 이 폴더와 맞는가
  canonical·og:url 이 그 파일의 URL 인가    hreflang 4줄이 같은 페이지의 다른 언어인가
  .langsw 가 같은 페이지의 다른 언어인가      푸터 4개가 자기 언어 content/ 인가
  sitemap.xml 이 페이지 목록과 정확히 일치하는가
  ```
  **존재 검사만으로는 부족하다** — 영어 페이지 푸터가 한국어 약관을 가리켜도 브라우저는 멀쩡히 연다. 언어 일치까지 같이 보는 것이 이 파일의 핵심이다. `npm test` 의 첫 단계이기도 하다.
- **빌드 경계는 이제 "다국어" 가 아니라 "20장" 이다.** 다국어(M3)로 가면 빌드가 필요할 거라고 적어뒀었는데, 엔진·화면 코드를 공유 파일로 빼니 **복사되는 로직이 0** 이 되어 3언어 18장까지는 빌드 없이 왔다.
  - 남은 중복은 푸터 6종과 `<head>` 뿐이다. **20장 경계가 코앞이다** — M4 도구 4개를 붙이면 3언어 × 4 = 12장이 더 붙어 30장이 된다. 그 시점에 다시 검토한다(기획서 6.1). 그전에 미리 만들지 말 것.
- **`--page` 와 `--bg` 를 한 변수로 합치지 말 것.** `--page`(#E6E6F2)는 본문 **바깥** 색이고, `--bg`(#E8EBEE)는 본문 **안쪽** 작은 부품 세 곳이 쓰는 연회색이다(`.lang` 언어 칩 · `.track` 확신도 막대 · `.chip.dim`). 합치면 바깥 색을 손볼 때마다 결과 화면의 칩이 같이 끌려간다. 확인법 — `var(--bg)` 가 **3건**이고 `body` 에는 없어야 한다.
  - **흰 종이는 `<main class="sheet">` 이고 본문 내용만 덮는다.** 맨 위 줄(`.topbar`)·제목(`header`)·푸터는 **바깥색 위에 남는다** — 그래야 "읽을 내용" 과 그 바깥이 갈린다. **`.wrap` 을 통째로 희게 칠하지 말 것.** 한 번 그렇게 했다가 되돌렸다 — 페이지 전체가 흰 판이 되어 갈라지는 자리가 아예 없어졌다.
  - 경계는 **선이 아니라 옅은 그림자**로 낸다. 흰색과 `--page` 는 밝기 차가 작아 선을 그으면 되레 지저분하다. 같은 이유로 `header` 의 아랫줄과 `footer` 의 윗줄을 **뺐다** — 종이의 위아래 모서리가 이미 그 역할을 한다. 되살리지 말 것.
  - `<main>` 은 18장 전부 **한 개씩**이고 `<footer>` 앞까지를 감싼다. 새 페이지를 만들 때 이 껍데기를 빠뜨리면 그 페이지만 흰 종이 없이 나온다.
  - **바깥색 위에 남는 것은 도구 페이지와 법적 페이지가 다르다.** 도구 페이지는 로고 + 설명 문장까지가 바깥이고 흰 종이는 도구 카드부터다. 법적 페이지는 로고까지만 바깥이고 **페이지 제목(`이용약관` 등)이 흰 종이 맨 위**다 — 그 제목은 읽을 내용의 일부이지 사이트 머리글이 아니다.
    ```
    도구:  .topbar(로고+언어) · header(설명)  →  main.sheet(카드·본문)  →  footer
    법적:  .topbar(로고+언어)                 →  main.sheet(h1·본문)    →  footer
    ```
    **법적 페이지에는 `<header>` 가 아예 없다.** 비어 버려서 지웠다 — 되살리지 말 것. 그 간격은 `.topbar + .sheet` 가 준다.
  - **흰 종이 위에서 `.card`·`.res` 는 1px 선만으로는 약해서** 같은 옅은 그림자를 하나씩 갖고 있다. 색(`--card` 흰색)은 그대로다 — 카드를 회색으로 낮추지 말 것.
  - **좌우 여백은 `clamp(18px,4vw,30px)` 다.** 넓은 화면에서 글이 종이 가장자리에 붙지 않게 하면서 좁은 화면에서는 18px 로 줄어든다 — `@media` 없이 해결되므로 미디어쿼리를 새로 넣을 이유가 되지 않는다.
- **CSS 원본은 `styles.css` 하나다.** `index.html` 안에 `<style>` 을 되살리지 말 것 — 원본이 두 곳으로 갈라진다.
  - 쪼개지도 말 것. 도구 전용 선택자 57개와 공용 25개가 섞여 있지만, 법적 페이지가 안 쓰는 규칙은 그냥 적용이 안 될 뿐이다. 파일을 나누면 관리할 곳만 늘어난다.
  - **`.tools`/`.tool` 은 허브 전용이다** — 도구 목록 카드. `.case` 와 생김새가 비슷하지만 성격이 다르다(`.case` 는 읽을 것, `.tool` 은 고를 것이라 블록 전체가 링크다). `.case` 로 대체하지 말 것 — 탭 타깃이 사라진다.
- **엔진을 여러 조각으로 쪼개지 말 것.** 기획서 6.1 의 `src/engine/*.js` 분리안 얘기다. 다만 **한 파일(`engine.js`)로 빼는 것은 이미 했다** — 아래 사유 참고. 조각내는 것과 통째로 옮기는 것은 다르다.
  - 원래 규칙은 "쪼개면 `test/sync.js` 가 깨져 벤치마크 4개가 멈추고, **사용자 이득이 0**" 이었다. 다국어에서는 언어별 페이지가 엔진을 공유해야 하므로 이득이 0이 아니고, 통째로 옮기면 `sync.js` 가 **필요 없어져서** 오히려 단순해진다. 규칙이 걱정한 손해가 이 모양에서는 안 생긴다.
  - 옮길 때 엔진·UI 코드를 **바이트 단위로 대조**해 한 글자도 안 바뀐 것을 확인했고, `npm test` 10개 숫자가 전부 그대로였다. 다시 손댈 때도 같은 방법으로 확인할 것.
- `sitemap.xml` 과 canonical 은 절대 URL 이라 **도메인(`prelaps.com`)이 박혀 있다.** 도메인을 바꾸면 18장의 `<head>` + `sitemap.xml` + `robots.txt` 를 전부 고쳐야 한다 — `test/links.js` 의 `ORIGIN` 상수도 같이 고칠 것.

## 광고 관련 (사이트화 이후)

- 복구 버튼 주변에 광고 슬롯 배치 금지 — 오클릭 유도는 애드센스 계정 정지 사유.
- 광고 슬롯은 `min-height` 사전 지정 (CLS 방지).
- 결과가 동적으로 갱신되는 컨테이너 **안에** 광고 넣지 말 것.

## 줄마다 다르게 깨진 입력 (`recoverLines`)

복구는 "어떻게 잘못 읽혔는지" 를 **하나** 가정하고 되감는다. 줄마다 가정이 다르면 통짜로는 못 푼다 — 한 줄에 `latin1-raw` 로 표현 못 하는 문자가 섞이는 순간 그 가설이 통째로 탈락하고 후보 전체가 엉뚱한 인코딩으로 밀려난다.

- **"여러 줄이라서 실패한다" 가 아니다.** 같은 유형이면 여러 줄이 **더 정확하다**(문장이 길수록 채점이 선다). 무너지는 조건은 줄 수가 아니라 **유형이 섞였을 때**다. 안내 문구를 쓸 때 이걸 혼동하지 말 것.
- **줄별 결과가 통짜보다 자연스러울 때만 채택한다.** 무조건 줄 단위로 나누면 짧아진 줄에서 정확도가 떨어져 잘 되던 문단이 망가진다.
- **랭킹에 섞지 않는다.** 줄마다 근거(chain)가 달라 카드 형식에 안 맞고, 판정 성격도 다르다 (부분 복구와 같은 원칙). 대신 이 카드가 뜨면 랭킹 확신도를 `참고용` 으로 낮춘다 — 통짜 1순위가 쓰레기인데 88% 가 붙는 것이 이 문제의 핵심이었다.
- **파일 탭에서는 `content` 판정보다 먼저 시도해야 한다.** `contentMojibake()` 는 통짜 `recover()` 결과라 섞인 입력에서는 그것도 쓰레기다. 순서를 바꾸면 줄별 복구에 도달하지 못한다.
- `npm run bench:node` 가 두 방향을 같이 잰다 — **문단 발동 0건**(회귀 감지선)과 섞임 하한 25%. 이득만 재면 정상 문단을 망가뜨리는 변경을 통과시키게 된다.

## 붙여넣다 잃은 NBSP (`repairNbsp`)

`제목`(U+C820)처럼 UTF-8 바이트에 `0xA0` 이 든 글자는 latin1 계열로 잘못 읽히면 깨진 문자열에 **NBSP(U+00A0)** 를 남긴다. 그런데 **메신저·HTML·웹 입력창·엑셀이 NBSP 를 일반 공백으로 조용히 바꾼다.** 사용자는 자기 입력이 손상됐다는 걸 알 방법이 없다.

실측: 148건 중 NBSP 가 든 것 **20건(14%)**, 고치기 전에는 그 20건이 **전부** 실패했다. 언어 편중도 없다(ko 4·ja 4·zh 4·fr 3·vi 3·th 1·el 1). 복원 후 **20/20 (100%)**.

- **C1 소실과 혼동하지 말 것.** C1 은 바이트가 진짜로 사라져 복구 불가지만(기획서 5.1.2), NBSP 는 **UTF-8 의 이어지는 바이트가 반드시 `0x80~0xBF`** 라는 구조 덕에 되살아난다. `0x20` 은 그 범위에 못 들어가므로 그 자리에 공백이 있다는 것 자체가 손상 신호다. **안내 문구도 달라야 한다** — 하나는 "복구 불가", 하나는 "되살렸다".
- **되돌릴 바이트를 `0xA0` 으로 박아두지 말 것.** 인코딩마다 다르다 — latin1·windows-1252·windows-1251 은 `0xA0` 이지만 **koi8-r 은 `0x9A`, ibm866 은 `0xFF`, macintosh 는 `0xCA`** 다. `0xA0` 으로 고정했더니 `utf-8→koi8-r` 7건이 한 글자씩 틀렸다(`안녕하세젔`, `会議`→`传議`). `nbspByte(Y)` 가 `encodeWith` 와 같은 표에서 뽑는다.
- **`bytes` 를 갈아치우지 말 것.** `ACTUAL` 20종이 같은 배열을 돌려 쓴다. 복원본은 `utf-8` 시도에만 쓰는 지역 변수에 담는다.
- **엄격 검증(`fatal:true`)을 통과할 때만 채택한다.** 이게 오탐 0의 근거다 — 정상 텍스트는 애초에 첫 검증을 통과하므로 이 경로에 오지 않는다.
- **복원했다는 사실을 감추지 말 것.** 근거 칩에 `공백 복원` 을 넣고 안내에 "파일로 올리면 이런 손상이 없습니다" 를 붙인다. 사용자가 다음번 대처를 배우는 자리다.
- `npm test` 하한 80% (현재 100%).

## 정상 글자가 섞인 입력 (`recoverSegments`)

**실사용자는 깨진 글자만 딱 떼어서 가져오지 않는다.** `제목: <깨짐> 입니다` · `<깨짐>_보고서.xlsx` 처럼 정상 글자와 섞어서 온다. 그 순간 `encodeWith` 가 되돌릴 수 없는 문자를 만나 가설을 통째로 버리므로 **후보가 0개**가 된다.

**벤치마크가 이걸 못 보고 있었다.** 148건은 깨진 글자만 떼어낸 합성 입력이라 82.4% 가 나오는데, 앞에 `제목: ` 만 붙이면 **0%** 다(후보 0개가 98%). ASCII 라벨 `name: ` 만 76.4% 로 멀쩡한 것이 원인을 그대로 말해준다 — `latin1-raw` 가 ASCII 는 표현할 수 있기 때문이다.

`recoverSegments()` 는 되돌릴 수 있는 구간만 골라 되돌리고 나머지는 손대지 않는다. `recoverLines` 가 줄 단위로 하는 일을 한 단계 아래로 내린 것이고, 새 채점 로직 없이 `recover()` 를 재사용한다.

- **`narrowMisread(s)` 로 구간을 나누지 말 것.** 그건 입력 **전체**를 보고 후보를 좁히는 함수라, 정상 한글이 섞여 있다는 이유만으로 `latin1-raw` 를 빼버린다 — 정작 깨진 구간을 되돌릴 유일한 가설이 그것인데도. 구간 분리 단계에서는 `MISREAD` 전체를 본다.
- **개선폭은 양쪽 다 raw `score()` 로 잰다.** `recover().list[0].score` 에는 `attempt()` 가 더한 `strictUtf` +70 과 길이 축소 가점(최대 30)이 섞여 있다. 이걸 `score(seg)` 와 직접 비교하면 정상 ` 입니다` 가 gain **197.8** 을 받아 교체된다. (`contentMojibake` 는 이 혼합값 그대로 `CONTENT_GAIN` 을 잡아둔 것이니 그쪽 상수를 여기 가져다 쓰지 말 것)
- **공백 단위(토큰) 분할은 실측으로 기각했다.** 커버리지는 넓지만(CJK·키릴 0→53%) 짧은 토큰에서 정상 단어를 오판한다 — `déjà vu à côté` → `蹤衪 vu à 饣蹴`. 5.1.2 의 "짧은 입력은 통계가 성립하지 않는다" 가 그대로 재현되는 것이라 임계값으로 못 막는다.
- **`SEG_ASCII_MAX` 가드를 빼지 말 것.** 없으면 베트남어 `Xin chào bạn` 의 `Xin chào b` 가 `utf-8→utf-16le` 로 읽혀 한자 110점을 받고 `楘⁮档濠戠ạn` 이 된다.
- **통짜가 답을 냈으면 아예 돌리지 말 것 (`wholeOk` 가드).** 이걸 빼먹고 한 번 나갔다 — 랭킹 1위는 정답인데 **그 위에 오답 카드가 얹히고** 확신도가 참고용으로 강등됐다. 카드가 `.res.best` 라 화면에서 가장 큰 자리다. 판정식은 `weak` 과 같은 것(`list[0].score >= base+10`)을 쓴다. 파일 탭에서는 `r.content` 유무가 같은 역할을 한다.
- **랭킹에 섞지 않는다.** `recoverLines` 와 같은 자리·같은 카드를 쓴다. 이 카드가 뜨면 랭킹 확신도를 `참고용` 으로 낮추는 것도 그대로다.
- 파일 탭에서도 `recoverLines` 와 같은 이유로 `content` 판정보다 먼저다.
- `npm test` 가 **세** 방향을 잰다 — 맥락 하한 50%, **안 깨진 입력 24건에서 카드 0건**, **통짜로 맞힌 케이스 위에 카드 0건**. 마지막 것이 위 회귀를 잡는 선인데, 처음엔 "통짜가 맞히면 통짜로 세고 아니면 구간을 시도한다" 로만 재서 사각지대였다. **이득만 재면 정답 위에 오답을 얹는 변경을 통과시킨다.**

## 결과 언어 라벨 (`langHint`)

후보 옆에 붙는 `한국어` `키릴 문자` 같은 라벨. **문자로 단정할 수 있는 것만 언어 이름을 쓴다.**

- **키릴·한자·라틴을 언어 이름으로 바꾸지 말 것.** 키릴 문자는 러시아어·우크라이나어·불가리아어가 공유하고, 한자만 있는 텍스트는 중국어·일본어·한국어 한자를 구분할 수 없으며, 라틴 악센트는 서유럽 여러 언어가 공유한다. `키릴 문자`·`한자`·`라틴 문자` 로 물러서 있는 것은 실수가 아니라 의도다.
- 인코딩(`ENC_LANG`)은 **스크립트로 판정이 안 될 때만** 보조로 쓴다. `gbk` 로 저장했으면 중국어권 문서일 가능성이 높다는 정도의 단서다.
- `npm run bench:node` 가 148건에서 라벨이 실제 언어와 모순되지 않는지 검사한다(하한 90%, 현재 97.3%). 어긋나는 4건은 가나 없는 일본어(`会議室A-301`)를 CJK 인코딩으로 저장한 합성 케이스로, **원리적으로 구분 불가**다.
- **부분 복구 섹션에서 특히 중요하다.** 그 후보들은 채점상 전부 동점이라(기획서 5.1.2) 순서가 판단 결과가 아니다. 라벨이 사실상 유일한 선택 근거이므로 빼지 말 것.

## 진단 임계값을 건드릴 때

`reasons()` 의 두 상수는 벤치마크로 잡은 값이다. 바꾸려면 다시 측정할 것.

- `SCORE_FLOOR=70` — 정답의 최저 점수가 75라서 그 바로 아래로 잡았다. 올리면 정답에 경고가 붙는다.
- `DMG_MAX_RATIO=0.25` — UTF-8 구조가 깨진 비율. 0 초과~0.25 구간은 정상 데이터가 **0건인 빈 구간**이라 분리선으로 쓴다. 0.25를 넘기면 EUC-KR 깨짐이 무더기로 걸려 오탐이 50건까지 뛴다.
- `CONTENT_GAIN=25` — 파일 인코딩은 정상인데 안에 담긴 글자가 깨진 경우를 잡는 선. `recover()` 의 개선폭 기준이다. 정상 텍스트 20건의 최대가 20, 깨진 텍스트 148건 중 137건이 25를 넘었다(오탐 0/20). **10으로 낮추면 안 된다** — 정상 한국어·일본어가 15~20을 받아 오탐이 8/20으로 뛴다.
- `LINE_GAIN=10` — 줄별 결과를 채택하는 기준(점수 차). 한 언어·한 유형 문단 23건에서 **이 조건이 성립한 것이 0건**이라 임계값 0~40 어디서도 개악이 없었다. 민감한 값이 아니므로 여유를 둔 것뿐이다. 유형 섞인 228건에서 1% → 26%.
- `MAX_LINES=60` — 줄마다 `recover()` 를 돌리므로 둔 상한. 넘으면 시도하지 않는다.
- `SEG_MIN=4` / `SEG_GAIN=25` — 구간 최소 길이와 교체 기준 개선폭. `SEG_GAIN` 은 양쪽 다 raw `score()` 로 잰 값이다(위 절 참고).
- `MAX_SEGS=200` — 구간마다 `recover()` 를 돌리므로 둔 상한(`MAX_LINES` 와 같은 성격). 서로 다른 조각이 반복되면 memo 가 안 먹혀 구간당 약 3.8ms 로 선형 증가한다 — 50구간 206ms / 800구간 3,071ms.
- `SEG_ASCII_MAX=0.6` / `LINE_ASCII_MAX=0.65` — ASCII 글자 비율 상한. **깨진 글자는 고바이트 문자이고 ASCII 는 어떤 오독을 거쳐도 그대로 통과하므로**, ASCII 가 대부분인 조각은 애초에 깨진 조각이 아니다. `asciiRatio()` 는 공유하지만 **임계값은 합치지 말 것** — 측정 범위가 거의 안 겹친다.
  - 구간: 정답을 낸 278건 최대 **0.58**(중앙 0.00) / 오작동한 베트남어 `Xin chào b` **0.67** → 0.67 미만이어야 한다
  - 줄별: 정상 독일어 줄 **0.86·0.84·0.70** / 깨진 줄 **0.00·0.33** → 0.65 이상이어야 섞임 정답률이 안 깎인다
  - 하나로 합치려다 섞임 26.3% → **24.6%** 로 떨어뜨려 하한(25%)을 깬 적이 있다. 겹치는 구간이 0.65~0.67 뿐이라 여유가 없다.
- **`recoverLines` 에도 이 가드가 필요하다.** 없으면 **안 깨진** 독일어 문단이 통째로 한자가 된다(`Bitte prüfen Sie die Unterlagen vorher` → `楂瑴⁥牰曼湥…`). 14차 이전부터 있던 버그였는데, `benchmark.js` 의 "문단 발동 0건" 이 *깨진* 문단만 보고 **안 깨진 문단을 한 번도 안 넣어서** 안 잡혔다. 줄별 개선폭 기준(`>=25`)으로는 못 막는다 — 정상 라틴 줄이 한자로 바뀌면 개선폭이 +40 이라 통과한다.
- `MOJI_HINT=0.5` — 라틴 보조 영역(U+0080~U+00FF) 비율. 결과를 바꾸지 않고 "왜 안 됐는지" 안내 문구를 고르는 데만 쓴다. 정상 텍스트 최대 0.36(악센트 많은 프랑스어), 깨진 텍스트 중앙값 0.82.

## CSS·모바일을 건드릴 때

> **좁은 폭을 잴 때 주의** — 헤드리스 크롬은 창을 **500px 아래로 못 줄인다**(`--window-size=320` 을 줘도 뷰포트가 485px 로 나온다). 배율(`--force-device-scale-factor`)로도 안 된다. `.wrap{max-width:320px}` 을 주입해 재면 실제 320px 화면의 콘텐츠 폭과 같아진다. 그리고 **크기 0인 요소를 빼고 세야 한다** — 숨겨진 탭 안의 요소는 rect 가 전부 0이라 "왼쪽으로 삐져나옴" 으로 잘못 잡힌다.

폭 320/360/390px × 5개 페이지를 headless Chrome 으로 실측해 잡아둔 상태다 — **삐져나옴 0건, 탭 영역 전부 44px 이상.** 아래 셋은 그때 실제로 물린 것들이라 되돌리지 말 것.

- **`@media` 가 없는 것은 미완성이 아니다.** 단이 하나뿐이고(`.wrap{max-width:900px}`), 가로줄은 전부 `flex-wrap:wrap` 이며, 넘칠 만한 것은 `.ex`·`.names`·`.text` 가 자기 안에서 스크롤한다. 미디어쿼리는 **2단을 1단으로 접을 때** 필요한 것인데 접을 2단이 없다. "반응형 추가" 라는 이유로 손대지 말 것 — 실제로 접어야 할 것(광고 슬롯·2단 레이아웃)이 생기면 그때 넣는다.
- **`textarea` 글자를 16px 아래로 내리지 말 것.** iOS 사파리는 글자가 16px 미만인 입력창을 누르면 화면을 자동 확대하고 **확대된 채로 돌아오지 않는다.** `viewport` 에 `user-scalable=no` 를 넣어 막는 우회는 접근성을 해치므로 쓰지 않는다.
- **한 클래스명을 두 용도로 쓰지 말 것.** `.copy` 가 복사 버튼(6곳)과 푸터 저작권(5곳)에 동시에 쓰여, 뒤에 온 정의가 이기는 바람에 **버튼이 의도와 다른 12px 모노스페이스로 그려지고 있었다.** 화면에 오류가 안 나서 오래 안 걸렸다. 푸터 쪽은 `.cr` 로 분리했다.
- **"작게" 와 44px 는 같이 지킬 수 있다.** 글자 크기만 줄이고 `min-height:44px` 로 누를 수 있는 높이를 따로 확보한다 — 보이는 크기와 탭 타깃은 별개다. `.langsw` 가 12px 글자에 44px 높이인 것이 그 예다.
- **지우기 버튼을 `.tabs` 안에 넣지 말 것.** 그 줄은 `role="tablist"` 라 탭이 아닌 버튼이 들어가면 스크린리더가 탭 개수를 잘못 읽는다. `.tabrow` 로 감싸 `.tabs` 의 **형제**로 둔다. 밑줄과 바깥 여백도 `.tabrow` 가 갖는다 — 안 옮기면 밑줄이 탭 글자 폭만큼만 그어진다.
- 눌러야 하는 요소(버튼·`.tab`·`.fnav a`·`.brand`·`.langsw a`·`#clear`)는 **높이 44px 이상**을 지킨다. 글자만 있는 링크는 `display:inline-block` + 세로 패딩으로 키운다. **본문 문단 속 인라인 링크는 예외** — 키우면 줄 간격이 들쭉날쭉해지고 구글도 문단 내 링크는 이 기준에서 뺀다.

## 하지 말 것

- 짧은 입력에서 확신도 %를 그대로 노출 — 10자 미만은 통계가 성립하지 않아 근거 없는 숫자다. (`diagnose()` 의 `short` 플래그로 처리 중이니 우회하지 말 것. 파일 모드도 `SHORT_LEN` 을 같이 쓴다)
- **이진 파일에 텍스트 후보를 내놓는 것.** `looksBinary()` 가드가 없으면 PNG 헤더 16바이트가 `‰PNG ÿØ«` 같은 결과로 확신도까지 달고 나온다. NUL 바이트가 있거나 제어문자가 5%를 넘으면 텍스트가 아니다.
- PDF 를 "복구 가능"으로 다루는 것 — PDF 깨짐의 대부분은 인코딩 불일치가 아니라 폰트 CMap 손상이라 원본 정보가 파일에 없다. 본문에 "왜 안 되는지"만 적어둔 상태다.
- `?` 나 `` 가 포함된 입력에 "복구 가능"이라고 표시 — 바이트가 소실된 것이라 원리적으로 불가능하다.
- **엔진이 `?` 를 만들어내는 것.** `encodeWith` 가 미매핑 문자에 `0x3F` 를 밀어넣던 코드를 6차에서 제거했다. 되살리지 말 것 — 바이트가 오염된 후보가 높은 점수를 받아 `"??"` 를 1순위로 내놓았다.
